import AppFooter from "./AppFooter";

/**
 * Footer — dashboard app shell footer.
 * Thin wrapper around the unified AppFooter (variant="dashboard").
 */
export default function Footer() {
  return <AppFooter variant="dashboard" />;
}
